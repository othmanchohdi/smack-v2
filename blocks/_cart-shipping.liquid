{% assign bl_stts = block.settings %}

{%- liquid
  assign show_free_shipping_bar = false
  if cart != empty and settings.show_calculator_free_shipping_message
    assign is_wholesale_customer = false
    if settings.wholesale_gate_enabled and customer and customer.tags contains settings.wholesale_customer_tag
      assign is_wholesale_customer = true
    endif
    unless settings.wholesale_hide_free_shipping and is_wholesale_customer
      assign show_free_shipping_bar = true
    endunless
  endif
-%}

{%- if show_free_shipping_bar -%}
  <div
    class="cart-shipping-message spacing-style {{ bl_stts.alignment }}"
    style="{% render 'spacing-style', settings: bl_stts %}"
    {{ block.shopify_attributes }}
  >
    <div class="cart-message">
      {% render 'free-shipping-component' %}
    </div>
  </div>
{%- endif -%}

{% schema %}
{
  "name": "t:names.shipping_notification",
  "tag": null,
  "settings": [
    {
      "type": "select",
      "id": "alignment",
      "label": "t:settings.alignment",
      "options": [
        {
          "value": "left",
          "label": "t:options.left"
        },
        {
          "value": "center",
          "label": "t:options.center"
        },
        {
          "value": "right",
          "label": "t:options.right"
        }
      ],
      "default": "left"
    },
    {
      "type": "header",
      "content": "t:content.padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-inline-start",
      "label": "t:settings.left",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-inline-end",
      "label": "t:settings.right",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    }
  ],
  "presets": [
    {
      "name": "t:names.shipping_notification"
    }
  ]
}
{% endschema %}
